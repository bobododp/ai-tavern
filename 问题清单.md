# 当前问题清单

## P0：收尾前必须解决

- 用户可见乱码：已重点清理 `core.js`、`ui-render.js`、`ui-events.js`、`story-runtime.js` 和项目文档，仍需按验收清单手动扫页面。
- 重复按钮事件：故事板按钮已统一走全局 `data-story-action` 入口，移除局部重复监听。
- 画廊渲染重复函数：`renderImageCard` 已收口为单一实现，避免后定义函数覆盖前定义函数。
- 空按钮：主页事件总结区域的无功能“总结”按钮已移除。

## P1：冻结前需要验证

- 故事板卡片标签：自定义故事、视角、成熟模式符号不应再出现乱码或 `[object Object]`。
- 主角信息：身份、状态、能力字段如果是对象，要显示可读文本，不能显示 `[object Object]`。
- 生图链路：确认弹窗、审查优化、生成、上下文插图、画廊展示、删除、打开文件夹都需要手动跑一遍。
- 世界书导入：同一个文件重复导入、换文件导入、AI 翻译写入需要确认不会串旧数据。
- 上下文压缩：压缩后主页面、故事设置、上下文预览里的摘要保持一致。
- 设置页保存：聊天模型、生图接口、叙事规则保存后刷新页面仍保留。

## P2：可以带入下一阶段

- 自动化测试不足：当前主要依赖 `node --check` 和手动验收。
- 模块边界仍偏原型：多个脚本通过全局函数协作，后续可逐步拆成更清晰的模块。
- API Key 仍是本地原型方案：本地代理解决了 CORS 和图片保存，但不是正式安全后端。
- 打包 App 方案：中期采用 Electron / Tauri 桌面 App，内置本地代理和图片目录；当前先不执行打包。
- 皮肤系统只预留接口：当前可基于 CSS 变量和 `data-skin` 扩展，尚未做完整皮肤管理器。

## 暂缓事项

- 正式账号、云端同步、数据库。
- 完整 App 打包、安装包和自动更新。
- 重新设计整套 UI 视觉。
- 大规模重构技术栈。
